

import '../handling_flutter_imports/handling_flutter_imports_screen.dart';

class DefaultComponent {
  static const BorderRadius defaultBorderRaduis =
      BorderRadius.all(Radius.circular(10));
  static const Radius defaultRadius = Radius.circular(20);

  static double horizontalToApplication = 2.w;

  static EdgeInsets paddingToAuthation =
      const EdgeInsets.symmetric(horizontal: 24);
}